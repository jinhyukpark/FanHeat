server:
  port: ${PORT:4000}

spring:
  application:
    name: fanheat-backend
  autoconfigure:
    exclude:
      - org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration

management:
  endpoints:
    web:
      exposure:
        include: health,info
